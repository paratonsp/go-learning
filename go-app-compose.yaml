version: '3'
services:
  go-web-app:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=suryaindomakmur.site
      - LETSENCRYPT_HOST=suryaindomakmur.site